<%= form_tag search_path, method: :get do %>

<div class="field">
<%= text_field_tag :q, @q, placeholder: (defined?(placeholder) ? placeholder : 'Search') %> &nbsp; <%= submit_tag 'Search' %>
</div>

<% end %>